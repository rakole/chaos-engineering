package com.dgupta.library.processor;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class WordWrapTest {
	/*
	 * private String sample; private String lineBreak;
	 * 
	 * @BeforeEach public void init() { lineBreak = "\r\n"; sample =
	 * "hello i am doing great, what are you " +
	 * "doing today, i do not want to tell you to " + "not do anything!"; }
	 * 
	 * 
	 * // @Test void wrapString() { WordWrap wordWrap = new WordWrap();
	 * System.out.println(sample); String str = wordWrap.wrapString(lineBreak,
	 * sample, 11); System.out.println(str);
	 * 
	 * }
	 */}